# Auto-sync data files — may contain account/contact PII from Salesforce
# (root-anchored so it only ignores /Data, NOT cockpit/js/data on case-insensitive filesystems)
/Data/
/Data/*.json

# Local environment / OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor temp files
*.tmp
*.bak
